import "./style.css";

const root = document.getElementById("app");

root.innerHTML = `
  <main class="page">
    <section class="panel">
      <h1>Cosmotop Desktop</h1>
      <p class="status">Terminal runtime is not wired yet.</p>
      <p class="hint">This scaffold validates Wails app startup and embedded frontend assets.</p>
    </section>
  </main>
`;
